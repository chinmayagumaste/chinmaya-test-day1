collection ={"1", "2", "3", "4"}
collection2 ={"4", "5", "6", "7"}

print(collection.union(collection2)) # union() method returns a new set that contains all the elements from both sets, without duplicates.
print(collection.intersection(collection2)) # intersection() method returns a new set that contains only the elements that are present in both sets.
print(collection.difference(collection2)) # difference() method returns